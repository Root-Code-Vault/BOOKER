package com.booker.booking_service.service.kafka.consumer;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Service;

import com.booker.booking_service.entity.Booking;
import com.booker.booking_service.exception.ItemNotFoundException;
import com.booker.booking_service.repository.IBookingRepository;
import com.booker.events.SeatReserved;

@Service
public class SeatEventConsumerService {

	@Autowired
	private IBookingRepository bookingRepository;

//	@KafkaListener(topics = "seat-events", groupId = "booking-service")
//	public void consumeSeatEvent(SeatReserved event) {
//		Booking booking = bookingRepository.findById(Long.valueOf(event.getBookingId().toString()))
//				.orElseThrow(() -> new ItemNotFoundException(
//						"Booking details with ID: " + event.getBookingId().toString() + " not found"));
////            .ifPresent(booking -> {
////                booking.setSeatId(Long.valueOf(event.getSeatId().toString()));
////                booking.setStatus(event.getReserved() ? "CONFIRMED" : "SEAT_UNAVAILABLE");
////                bookingRepository.save(booking);
////            });
//		
//		if (event.getReserved()) {
//			booking.setStatus("CONFIRMED");
//		} else {
//			booking.setStatus("FAILED");
//		}
//		// if payment is done then we have to reverse it.
//		bookingRepository.save(booking);
//	}
	
	@KafkaListener(topics = "book-seat", groupId = "")
	public String consumeSeatEvents() {
		
		return null;
	}
}
