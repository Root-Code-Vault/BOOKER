spring:
  application:
    name: seat-service
#  config:
#    import: optional:configserver:http://localhost:9092
#  profiles:
#    active: default  # Replace 'default' with your actual profile name if needed
  datasource:
    url: jdbc:postgresql://localhost:5003/seatdb
    username: admin
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  kafka:
    bootstrap-servers: localhost:9092 # external listener, 👉 If running inside Docker, use `kafka:29092`.
    properties:
      schema.registry.url: http://localhost:8081
    consumer:
      properties:
        avro.reader: true
      group-id: seat-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: io.confluent.kafka.serializers.KafkaAvroDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: io.confluent.kafka.serializers.KafkaAvroSerializer

#  data:
#    redis:
#      host: localhost
#      port: 6379
#      database: 1
#  cache:
#    type: redis

logging:
  level:
    root: info
    org.springframework.web: DEBUG
    org.springframework.web.servlet.DispatcherServlet: DEBUG
    org.springframework.web.filter.CommonsRequestLoggingFilter: DEBUG
    org:
      springframework:
        jdbc: DEBUG
        orm: DEBUG

server:
  port: 9096

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka/
    register-with-eureka: true
    fetch-registry: true